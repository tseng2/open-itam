# Open-ITAM 开发者与 AI 智能体编码规范指南 (AGENTS.md)

> 本规范是 Open-ITAM 项目所有贡献者（包括开发人员与 AI 编码智能体）必须严格遵守的工程守则。
> 任何代码提交、重构或架构演进均需对照本指南逐项自查。

---

## 目录
1. [核心设计哲学](#1-核心设计哲学)
2. [代码简洁与整洁之道](#2-代码简洁与整洁之道)
3. [系统健壮性与容错机制](#3-系统健壮性与容错机制)
4. [严禁硬编码与配置驱动原则](#4-严禁硬编码与配置驱动原则)
5. [系统与数据安全性规范](#5-系统与数据安全性规范)
6. [架构分层与依赖规范](#6-架构分层与依赖规范)
7. [API 设计与前后端契约](#7-api-设计与前后端契约)
8. [Git 与提交规范](#8-git-与提交规范)

---

## 1. 核心设计哲学
- **KISS (Keep It Simple, Stupid)**：优先编写直观、清晰、易于理解的代码，拒绝无谓的过度工程与复杂的层层包装。
- **职责单一 (SRP)**：每个函数、模块或结构体只做好一件事。函数长度尽量控制在 50 行以内。
- **事实唯一源 (Single Source of Truth)**：数据库模型、协议定义与路由规范以项目契约文件为准，严禁在多处维护重复的字典或映射。

---

## 2. 代码简洁与整洁之道
- **消灭冗余逻辑**：
  - 避免深层嵌套（超过 3 层的 `if/else` 或循环应通过卫语句 `Guard Clauses` 提前返回）。
  - 禁止无意义的代码复制粘贴；公共逻辑必须提炼为清晰的工具函数或中间件。
- **自解释命名 (Self-Descriptive Naming)**：
  - 变量名、函数名与结构体必须表意清晰，严禁使用 `a`, `b`, `tmp1`, `flag2` 等模糊命名。
  - Go 语言命名遵循官方规范：包名简短小写；对外暴露大驼峰，内部小驼峰；错误变量以 `Err` 开头。
  - 前端命名规范：Vue 组件使用大驼峰（如 `DeviceDetail.vue`），文件与目录命名小写连字符或驼峰统一。
- **代码注释与文档同步**：
  - 注释应当解释“为什么这么做”（业务背景、设计原因、兼容考虑），而非“做了什么”（代码本身已自解释）。
  - 修改已有接口或模型时，必须同步更新对应的文档与类型定义。

---

## 3. 系统健壮性与容错机制
- **错误处理闭环 (No Ignored Errors)**：
  - Go 中除特定无害操作（如释放只读 Close）外，**严禁使用 `_ = err` 吞掉错误**。
  - 必须对底层系统调用（如 WMI 查询、注册表读取、网络请求、JSON 反序列化）进行显式错误捕获，并附带上下文日志记录（例如 `fmt.Errorf("fetch disk smart: %w", err)`）。
- **Agent 端静默容错与降级**：
  - Agent 常驻于企业终端设备，**任何采集模块（CPU、SMART、软件列表等）发生异常时，严禁引起主程序崩溃或 Panic**。
  - 采集应采用多级降级策略（如：WMI 失败自动降级到 PowerShell/系统命令；命令不可用时上报安全默认值）。
- **防止资源泄漏**：
  - 所有网络连接、文件句柄、数据库事务与 Body 流，必须通过 `defer` 或显式关闭机制确保回收。
  - 必须对跨节点 HTTP 请求、数据库长查询与系统命令执行设置合理的 **Timeout 超时控制**。

---

## 4. 严禁硬编码与配置驱动原则
- **拒绝“魔法值” (No Magic Numbers/Strings)**：
  - 业务状态码、事件类型、角色标识（如 `super_admin`, `admin`, `user`）、设备状态（`10:库存`, `20:在用` 等）必须统一定义为常量（`const`）或枚举，禁止直接在业务逻辑中散落字符串与数字字面量。
- **配置与数据分离**：
  - 端口号、外部系统网关（用友 U8、AD 域控地址）、认证密钥、采集心跳周期、超时阈值等，必须通过配置文件（`server.json` / `agent.json`）或环境变量注入，严禁硬编码在源代码中。
- **数据库路径与目录动态获取**：
  - 避免硬编码固定盘符路径（如 `E:\...` 或 `C:\...`），必须通过工作目录、相对路径或可配置参数解析。

---

## 5. 系统与数据安全性规范
- **凭据与敏感数据零泄漏**：
  - **严禁将任何密码、Token、私钥、API 凭据提交至 Git 仓库**。
  - 涉及安全性的临时文件、构建包或测试脚本，必须在提交前加入 `.gitignore`。
- **身份认证与权限隔离 (RBAC)**：
  - 服务端对外接口必须经由统一的 `AuthMiddleware`（JWT 校验）与 `RoleMiddleware` 进行访问控制。
  - Agent 上报接口与管理员管理后台接口在网络协议与鉴权层面保持逻辑/通道隔离。
- **防御注入与安全编码**：
  - 数据库查询一律使用参数化查询或 ORM 预编译绑定（如 GORM `Where("username = ?", name)`），严禁字符串拼接 SQL。
  - 终端命令执行（PowerShell/Bash）需进行参数安全转义与白名单校验，杜绝命令注入风险。
  - 密码必须通过不可逆哈希算法（如 `bcrypt`，加盐哈希）存储，严禁明文持久化。

---

## 6. 架构分层与依赖规范
- **清晰的分层边界**：
  - `cmd/`：仅包含程序启动入口与配置加载，不写任何业务逻辑。
  - `internal/server/model/`：纯粹的数据模型定义，避免引入外部网络或框架强依赖。
  - `internal/server/store/`：数据库交互与持久化，上层业务通过 Repository 接口解耦。
  - `internal/server/api/`：负责 HTTP 请求的路由、反序列化、入参校验与响应组装。
  - `internal/agent/collector/`：按平台隔离的底层信息采集器，保持零外部第三方 CGO 依赖。
- **多租户与公司数据隔离**：
  - 任何属于公司维度的实体（资产、部门、员工），数据库模型必须带有 `CompanyID` 索引字段。
  - 查询与修改逻辑必须带有公司边界校验，防止越权（Horizontal Privilege Escalation）。

---

## 7. API 设计与前后端契约
- **RESTful 风格与响应标准化**：
  - 统一 JSON 响应格式：
    ```json
    {
      "code": 0,
      "message": "success",
      "data": {}
    }
    ```
  - 成功返回 `code: 0`；失败返回非零业务错误码并附带人机皆可读的 `message`。
- **分页规范**：
  - 列表查询接口统一支持 `page` 与 `page_size` 参数，返回格式统一包含 `items` 数组与 `total` 总记录数。
- **前端状态与路由保护**：
  - 前端路由守卫必须与后端 HTTP `401 Unauthorized` 拦截器联动，妥善处理 Token 失效并优雅回跳至登录视图。

---

## 8. Git 与提交规范
- **清晰的 Conventional Commits**：
  - `feat:` 新功能（如：`feat: add user management rbac`）
  - `fix:` 缺陷修复（如：`fix: resolve token mismatch on password update`）
  - `refactor:` 代码重构（不改变功能与外部行为）
  - `docs:` 文档更新
  - `chore:` 构建过程、依赖变更或配置清理
- **小步提交与原子性**：
  - 每个 Commit 应尽量保证原子性（完成一个独立小闭环），便于 Code Review 与版本回滚。
- **本地敏感产物清理**：
  - 提交前检查 `git status`，确保无临时日志（`*.log`）、打包归档（`*.zip`, `*.tar.gz`）、IDE 私有配置（`.zcode/`）等残留文件进入暂存区。
